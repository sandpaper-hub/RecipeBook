package com.example.recipebook.presentation.ui.collectionDetailScreen.model

import com.example.recipebook.R

enum class MenuItem(val stringResource: Int) {
    EDIT(stringResource = R.string.edit_text),
    DELETE(stringResource = R.string.delete_text)
}